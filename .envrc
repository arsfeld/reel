use flake

# Optional: Add project-specific environment variables here
# export RUST_LOG=debug
# export DATABASE_URL=sqlite://./data/cache.db

# Optional: Create data directories if they don't exist
mkdir -p data/cache
mkdir -p data/config

# Optional: Watch additional files for automatic reload
watch_file Cargo.toml
watch_file Cargo.lock